# Golden eval artifacts (per-run / large / regenerated)
golden/*/labels.jsonl
golden/*/crops/
eval/golden/*/labels.jsonl
eval/golden/*/crops/

# Per-run ledger
ledger.jsonl

# Per-run raw model responses (re_score artifact)
raw_responses.jsonl
eval/golden/*/raw_responses.jsonl

# Python bytecode
__pycache__/
*.pyc
_serve/

# Per-run search output (regenerated by lab run --pipeline search)
eval/golden/search/search_results.jsonl

# oh-my-claudecode session state
.omc/
