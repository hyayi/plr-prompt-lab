"""Eval-support layer — dataset, validate, provenance (공유 계약).

scoring·report·gallery 는 서버 전용이라 별도 레포(~/plr-eval-server)로 이관됨.
여기 남은 셋은 클라이언트 run/submit 과 서버 채점이 함께 쓰는 공유 파일 —
contract/CONTRACT.md 참조."""
